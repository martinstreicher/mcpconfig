<span class="inline-flex items-center gap-1.5 rounded-full px-2.5 py-0.5 text-xs font-medium <%= accent(scope.accent, :chip) %>"
      title="<%= title %>">
  <span class="h-1.5 w-1.5 rounded-full <%= accent(scope.accent, :bar) %>"></span>
  <%= scope.name %>
</span>
